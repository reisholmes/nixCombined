#!/usr/bin/env bash
# fkill - Kill processes interactively
# Uses fzf to select process(es) to kill

pid=$(ps -ef | sed 1d | fzf -m | awk '{print $2}')

if [ "x$pid" != "x" ]; then
  echo "$pid" | xargs kill -${1:-9}
  echo "Killed process(es): $pid"
else
  echo "No process selected"
fi
